**Avatar** — expert monogram (initials, not a photo). Keeps the experience scientific rather than social.

```jsx
<Avatar name="Dr. Elena Marchetti" size={40} />
<Avatar name="Hideo Tanaka" tone="evidence" />
```

Props: `name` (initials derived), `size` (px, default 36), `tone` (`auto | accent | evidence | neutral`).
